import { AddDollarSignPipe } from './add-dollar-sign-pipe';

describe('AddDollarSignPipe', () => {
  it('create an instance', () => {
    const pipe = new AddDollarSignPipe();
    expect(pipe).toBeTruthy();
  });
});
